# Vendored browser dependency

`supabase.js` is the UMD browser bundle from `@supabase/supabase-js` 2.112.2. It is stored locally so the private dashboard does not depend on a third-party CDN and can use a strict content security policy.

The upstream license is included in `supabase.LICENSE.txt`.
